# DUNGEON ESCAPE — 프로젝트 컨텍스트

3D 미로 탈출 호러 게임. 브라우저에서 동작하는 PWA.
배경은 **백룸(Backrooms)** — 겨자색 벽지, 축축한 카펫, 형광등이 늘어선 텍스 천장.
바닐라 JS + Three.js r128, **빌드 도구 없이** 네이티브 ES 모듈로 구성.

- 저장소: `mazeMonster`
- 배포: GitHub Pages (`.github/workflows/deploy.yml` — main push 시 자동 배포)
- 개발 환경: **주 개발 기기가 아이폰**. 데스크톱 IDE를 쓸 수 없는 경우가 많음.

---

## 파일 구조

```
index.html          HTML + CSS + 메인 스크립트 (약 1,370줄)
js/constants.js     밸런스·치수 상수
js/quality.js       품질 프리셋·자동 감지
js/maze.js          미로 생성 + BFS/LOS
js/textures.js      캔버스 텍스처 생성
js/audio.js         공간 오디오 전체
manifest.json       PWA 매니페스트
js/save.js          진행 저장 (localStorage)
sw.js               Service Worker (캐시명 dungeon-escape-v28)
icon-192.png        아이콘 (미로 그림)
icon-512.png        아이콘 (미로 그림)
README.md
.github/workflows/deploy.yml
```

---

## 0. 기본 작업 원칙 (모든 규칙에 앞서는 기준)

> Behavioral guidelines to reduce common LLM coding mistakes.
> Merge with project-specific instructions as needed.
>
> **Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

아래는 이 프로젝트뿐 아니라 모든 작업의 바탕이 되는 기준입니다.
이어지는 성능 규칙·품질 시스템 문서는 이 원칙 위에 얹히는 **프로젝트 고유 사항**입니다.

### 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

### 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: *"Would a senior engineer say this is overcomplicated?"* If yes, simplify.

### 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it — don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

**The test:** Every changed line should trace directly to the user's request.

### 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:

```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently.
Weak criteria ("make it work") require constant clarification.

---

> **These guidelines are working if:** fewer unnecessary changes in diffs,
> fewer rewrites due to overcomplication, and clarifying questions come
> before implementation rather than after mistakes.

---

## ⚠️ 최우선 규칙 — 성능 관련 (반드시 지킬 것)

이 프로젝트는 **실측을 통해 병목을 규명한 이력**이 있습니다. 아래 규칙을 어기면 프레임이 무너집니다.

### 1. 동적 광원 개수를 늘리지 말 것

**이 프로젝트 최대의 성능 함정입니다.**

Three.js forward 렌더링은 **픽셀마다 모든 광원을 순회**합니다.
과거 횃불마다 `PointLight`를 붙여 광원이 64개가 되었고, 그 결과:

```
FPS 13 (76.3ms) / DRAW 7 / TRIS 0.1k / LIGHT 64
```

**삼각형 100개를 그리는 데 76ms**가 걸렸습니다. 지오메트리는 무관했고 광원이 단독 원인이었습니다.

현재는 **광원 풀링** 방식으로 해결되어 있습니다.

- `panelData[]` — 모든 천장등의 위치를 데이터로만 보관
- `panelLightPool[]` — 실제 `PointLight`는 `MAX_PANEL_LIGHTS`(프리셋이 결정, 2~6)개만 존재
- `updatePanelLights()` — `Q.panelInterval`마다 플레이어에게 가장 가까운 천장등 위치로 풀을 재배치

새 광원을 추가하려면 반드시 이 풀 구조를 재사용하십시오. 개별 `PointLight` 생성 금지.

**실측 근거** (SwiftShader 소프트웨어 렌더링, 동일 미로·동일 지점):

| 프리셋 | LIGHT | DRAW | TRIS | FPS |
|---|---|---|---|---|
| LOW | 5 | 7 | 0.1k | 60 |
| HIGH | 9 | 53 | 0.8k | 20 |
| ULTRA | 11 | 9 | 0.1k | 4 |

지오메트리 지표는 사실상 동일한데 FPS만 15배 차이가 납니다. 광원 개수가 단독 변수입니다.

### 2. 광원을 끌 때 `visible = false`를 쓰지 말 것

Three.js는 **보이는 광원 개수가 바뀌면 셰이더 프로그램을 재컴파일**합니다.
매 프레임 `visible`을 토글하면 hitching이 발생합니다.

```js
// 금지
light.visible = false;

// 올바름 — 개수는 고정, 세기만 0
light.intensity = 0;
light.position.set(0, -9999, 0);
```

출구 광원도 이 방식으로 거리 기반 제어하고 있습니다.
(몬스터 눈 광원 풀은 개체가 눈 없는 검은 실루엣이 되면서 사라졌습니다.)

### 3. 추정으로 최적화하지 말 것

초기 진단에서 draw call 1,192개를 근거로 지오메트리 병합을 최우선 과제로 제시했으나,
실측 결과 프러스텀 컬링이 이미 대부분을 걸러내어 **실제 draw call은 7개**였습니다.
병합 작업을 먼저 했다면 효과가 거의 없는 곳에 시간을 쓸 뻔했습니다.

**성능 작업 전에는 반드시 화면 좌상단 계측 오버레이로 실측하십시오.**

> ⚠️ **단, 이 "draw call 7개"는 25×25 기준이었습니다.** 미로를 41×41로 키우자
> 같은 조건에서 **draw call이 494개**가 되어 결론이 뒤집혔습니다(→ 5번 항목).
> 실측 결과에는 항상 **어떤 조건에서 쟀는지**를 같이 적어 두십시오.

### 4. 기타 성능 관련 기존 처리 (되돌리지 말 것)

| 항목 | 현재 상태 | 이유 |
|---|---|---|
| `shadowMap` | 비활성 | 모바일에서 비용 과다 |
| `antialias` | 프리셋이 결정 | HIGH/ULTRA만 on |
| `pixelRatio` | 프리셋 상한 × `renderScale` | fill rate 절감 |
| BFS 버퍼 | 전역 재사용 (`_bfsVis` 등) | 매 호출 재할당 시 GC 스터터 |
| BFS 큐 | 순환 버퍼 (`_bfsQx/_bfsQz`) | `Array.shift()`의 O(n) 제거 |
| 미니맵 지형 | 오프스크린 캔버스 + **증분 갱신** | 새로 밝혀진 칸만 덧그림(`_mmNew`). 전체 재생성은 41×41에서 1,681칸을 걸을 때마다 훑음 |
| 시야 계산 | 이동·회전 시에만 | `_lastVisGx/_lastVisGz/_lastVisYaw` |
| `cachedPortal` 등 | 참조 캐싱 | 매 프레임 `worldObjs.find()` 금지 |

### 5. 지오메트리는 "청크 단위"로 병합할 것

41×41에서 칸마다 Mesh를 만들면 씬 객체 3,023개 / draw call 494개가 됩니다.
그렇다고 **전부 한 덩어리로 합치면 절두체 컬링이 통째로 죽어** 미로 전체가 매 프레임
그려집니다(TRIS 0.1k → 17.9k). `CHUNK`(현재 6)칸 단위로 나눠 합쳐야 둘 다 잡힙니다.

실측 (41×41, 동일 시드·동일 경로 40샘플):

| 청크 | 씬 객체 | DRAW | TRIS |
|---|---|---|---|
| 없음 | 3,023 | 494 | 7.7k |
| 4칸 | 654 | 124 | 10.2k |
| **6칸 (현재)** | **394** | **80** | **12.0k** |
| 12칸 | 229 | 46 | 17.7k |

r128의 `BufferGeometryUtils`는 `examples/` 쪽이라 코어 모듈에 없습니다. CDN 의존을
늘리지 않으려고 `mergeParts()`로 직접 합칩니다. `position`/`normal`/`uv`를 가진 인덱스
지오메트리면 종류가 달라도 됩니다. 부수 효과로 **레벨 로딩이 빨라졌습니다**
(Mesh 수천 개 생성 31~128ms → 병합 10~17ms).

던전 시절에는 횃불 불꽃(`torchFlames`)만 매 프레임 `scale`이 흔들려 병합할 수 없었습니다.
백룸의 천장 형광판은 크기가 변하지 않아 **전부 병합됩니다** — 병합 불가 객체가 하나도 없습니다.
(실측: 41×41에서 씬 객체 394 → 303, DRAW 80 → 48, TRIS 12.0k → 6.8k)

> ⚠️ **SwiftShader 벤치마크는 병합을 과소평가합니다.** 위 표를 잰 헤드리스 환경은
> GPU 없이 CPU로 래스터화하므로 삼각형 증가가 실제보다 훨씬 크게 손해로 잡힙니다.
> 병합 직후 이 환경의 FPS는 59 → 12로 떨어져 개악처럼 보였습니다.
> **실기기(아이폰) 실측 결과 HIGH 프리셋에서 41×41이 59~60fps로 상한을 유지합니다.**
> 삼각형 1만 개는 GPU에게 사실상 공짜이고, 줄어든 draw call·객체 수만 이득으로 남습니다.
> 앞으로도 지오메트리 양이 걸린 판단은 **반드시 실기기 수치로 확정하십시오.**

---

## 모듈 구성

**빌드 도구는 여전히 없습니다.** 브라우저 네이티브 ES 모듈이라 `<script type="module">`이
그대로 `./js/*.js`를 불러옵니다.

분리 기준은 **공유 가변 상태가 없는 것부터**입니다. `MW`/`MH`/`mazeGrid`/`P`처럼 매 레벨
바뀌고 여러 곳이 함께 읽는 상태는 **`index.html`(main)이 소유**합니다.
ES 모듈은 `import`한 바인딩에 대입할 수 없어서, 이런 상태를 모듈로 옮기면 전부
객체 프로퍼티나 setter로 바꾸는 대규모 재작성이 필요합니다. 테스트가 없는 상태에서
그 작업은 위험 대비 이득이 없습니다.

그래서 `maze.js`의 BFS/LOS는 **`(grid, MW, MH, ...)`를 인자로 받습니다.**
`textures.js`는 `anisotropy`를, `audio.js`의 리스너 동기화는 플레이어 좌표를 인자로 받습니다.

> 몬스터·월드 빌드·게임 루프는 공유 상태 의존이 커서 main에 남겼습니다.
> 더 쪼개려면 상태 소유 구조부터 바꿔야 합니다.

**파일을 추가하면 `sw.js`의 `ASSETS` 목록에 반드시 넣으십시오.** 빠지면 오프라인에서 깨집니다.

> **분리할 때 반드시 확인할 것** — 옮긴 코드가 쓰던 변수를 main이 계속 참조하는지.
> `_lastHeartbeat`(심장박동 박자)가 `audio.js`로 딸려 갔는데 게임 루프가 여전히 참조해
> `ReferenceError`가 났습니다. **몬스터가 7칸 안에 들어와야 실행되는 코드라
> 대부분의 테스트를 통과했고, 6회 중 1회꼴로만 재현됐습니다.**
> 분리 후에는 각 모듈의 최상위 선언 이름을 모아, main이 import도 재선언도 하지 않은 채
> 참조하는 것이 있는지 **정적으로 전수 검사**하십시오.

---

## 백룸 — 빛이 반전된 세계

던전에서 백룸으로 바꾼 것은 색칠이 아니라 **조명 구조의 반전**입니다.
이 차이를 모르고 값을 만지면 금방 던전으로 되돌아갑니다.

| | 던전 (이전) | 백룸 (현재) |
|---|---|---|
| 기본값 | **어둠**, 횃불이 예외적으로 밝힘 | **밝음**, 천장등 없는 구역이 예외적으로 어두움 |
| 밝기의 주역 | 횃불 `PointLight` | `AmbientLight` |
| `ambient` 역할 | 횃불 사이를 메우는 보정값 | **어두운 구역의 밝기를 단독 결정** |
| 안개 색 | 검푸른 `0x080a10` | 어두운 올리브 `0x272415` |
| 병합 불가 객체 | 횃불 불꽃 (scale 흔들림) | **없음** |

**안개를 노란색으로 만들지 마십시오.** 백룸의 노란 인상은 근경의 벽지가 만듭니다.
안개를 밝게 하면 천장등이 하나도 없는 복도에서 원경이 오히려 환해져 어둠이 성립하지 않습니다.
참고 사진들에서도 원경은 전부 어둠에 잠겨 있습니다.

### 어두운 구역 — `isDarkZone(col, row)`

천장등이 죽은 구획입니다. **칸 단위로 무작위하면 얼룩덜룩해질 뿐**이라
`DARK_BLOCK`(5칸) 덩어리로 묶어 "복도 하나가 통째로 어둡다"가 되게 합니다.
`level`을 해시에 섞어 레벨마다 배치가 달라지되 같은 레벨 안에서는 결정적입니다.
25×25에서 길 칸의 약 17%가 어두운 구역이 됩니다.

**어두운 구역은 장식이 아니라 게임플레이입니다** — 검은 몬스터가 거기서 보이지 않습니다.
밝은 곳에서는 멀리서부터 실루엣이 보여 압박을 주고, 어두운 곳에서는 발소리·으르렁만
남습니다. 공들여 맞춰 둔 3D 오디오가 여기서 유일한 정보가 됩니다.

### 천장이 emissive를 갖는 이유

천장등이 천장과 거의 같은 평면에 있어 입사각이 스치듯 들어갑니다(N·L≈0).
그래서 조명을 사실상 못 받아 천장이 어둡게 나옵니다. 물리적으로는 맞지만 참고 사진의
흰 천장이 안 나옵니다. `mCeil`에만 `emissive`를 줘서 해결합니다 — 광원을 늘리지 않는 축입니다.

단, **어두운 구역에는 `mCeilDark`(emissive 없음)를 씁니다.** 같은 재질을 쓰면 천장등이
하나도 없는 복도인데 천장만 하얗게 빛나 어둠이 통째로 무너집니다.
재질만 나누면 되고 병합 구조도 광원 개수도 그대로입니다.

### 정전과 손전등

레벨 전체의 활성 천장등이 무작위 타이밍에 한꺼번에 꺼졌다가 일정 시간 뒤 복구되는
이벤트입니다. 지금 있는 "개별 등의 22%가 살짝 깜빡이는" 미세 스터터와는 별개의,
훨씬 큰 규모입니다.

`blackoutT` 하나로 세 구간을 표현합니다 — **음수면 평상시**, 0 이상이면 정전 시작 후
경과 시간(초)입니다. `FLASH_DELAY`(1초) 전까지는 빛도 손전등도 없는 완전한 암전이고,
그 뒤 `BLACKOUT_DURATION`(12초)까지는 손전등만 켜집니다. 값 하나로 상태를 표현하니
"몇 초째인가"만 물으면 되고, 별도의 상태 열거형이나 두 번째 타이머가 필요 없습니다.

`AmbientLight`는 건드리지 않습니다. 정전 중에도 어두운 구역과 같은 밝기(29~31)는
유지됩니다 — 완전한 칠흑은 아닙니다. 최우선 규칙 3번(게임 밸런스는 프리셋에 넣지
않음)과 같은 이유로, 조명을 담당하는 값을 스크립트 이벤트라고 이것저것 건드리면
전체 밝기 기준이 흔들립니다.

**손전등(`flashlight`)은 광원 "개수"가 늘어나는 유일한 예외입니다.** 최우선 규칙 1번을
지키기 위해 부팅 시점에 딱 하나만 만들어 `camera`의 자식으로 두고, 평소엔
`intensity=0`으로 꺼 둡니다(규칙 2번 — `visible`을 토글하지 않습니다). `target`도
`camera`의 자식(로컬 `(0,0,-1)`)이라 카메라가 매 프레임 이동·회전할 때 광원과
조준점이 자동으로 따라옵니다. `P.yaw`/`P.pitch`를 읽어 방향을 계산하는 코드가
따로 필요 없습니다.

계측 패널의 LIGHT 수는 `scene.traverse`로 광원 객체 자체를 세므로, 정전이 아니어도
손전등 1개가 상시 잡힙니다(HIGH 기준 6 → 7). 실제 밝기 기여는 0이지만 픽셀당 광원
순회 비용은 그대로 발생하므로, **실기기에서 프레임을 반드시 재확인하십시오.**

`shadowMap`이 꺼져 있어 손전등도 벽을 그대로 통과합니다 — T자 갈림길에서 옆 복도
벽이 살짝 밝아지는 정도는 감수합니다. 몬스터는 `MeshBasicMaterial`이라 어떤 빛도
받지 않으므로, 손전등으로 몬스터 자체를 비출 수는 없습니다. 손전등이 밝힌 벽·바닥을
배경으로 검은 형체가 지나가는 실루엣만 보입니다 — 어두운 구역의 가시성 설계
(실측 0.71%)와 같은 성격입니다.

몬스터의 `seesPlayer()`는 격자 거리·LOS만 보고 조명과 무관하므로, 정전이 몬스터의
탐지 능력을 바꾸지는 않습니다. 어두운 구역과 마찬가지로 **플레이어가 몬스터를
보기 어려워질 뿐**입니다.

> ⚠️ **발생 확률·쿨다운·지속시간은 전부 초기 제안값입니다.** `BLACKOUT_DURATION`,
> `FLASH_DELAY`, `BLACKOUT_COOLDOWN_MIN/VAR`, `BLACKOUT_CHANCE_PER_SEC` 모두
> 실기기에서 체감하며 조정할 값입니다. 손전등의 밝기·각도(`FLASH_INTENSITY`/
> `FLASH_ANGLE`)도 마찬가지입니다.

---

## 핵심 시스템

### 미로 생성 — `generateMaze(W, H)`
Recursive Backtracking으로 완전 미로 생성 후, **내부 벽의 18%를 추가 제거**하여 루프를 만듭니다.
따라서 탈출 경로가 여러 갈래입니다.

**크기는 레벨마다 커집니다** — `mazeSizeFor(lv)` = 25 → 29 → 33 → 37 → **41 상한**.
재귀 백트래킹은 홀수 크기를 전제하므로 `MAZE_STEP`도 **반드시 짝수**여야 합니다.

`MW`/`MH`는 상수가 아니라 `let`입니다. BFS 버퍼는 `MAZE_MAX²`로 잡아 두고
현재 `MW`로 인덱싱합니다(`z*MW+x`). **버퍼를 `MW*MH`로 잡으면 레벨이 오를 때 넘칩니다.**

### 시작·출구 배치 — `pickStartAndExit()`

둘 다 무작위입니다. 다만 **직선거리로 고르면 안 됩니다** — 벽 하나를 사이에 두고
붙어 있을 수 있습니다. `bfsDistances()`로 실제 이동 거리를 구한 뒤,
가장 먼 지점의 **75% 이상** 떨어진 칸 중에서 출구를 뽑습니다.
실측상 시작–출구 거리가 30~76칸으로 나옵니다.

몬스터도 같은 거리 맵을 재사용해 `max(8, MW*0.35)`칸 밖에만 스폰합니다.
시작하자마자 눈앞에 있으면 손쓸 도리가 없습니다.

시작 시 플레이어는 **뚫린 쪽을 바라봅니다**. 전방 벡터가 `(-sin, -cos)`이므로
`yaw = atan2(-dx, -dz)`입니다.

### 몬스터 AI — `class Monster`
3단계 FSM으로 동작합니다.

| 상태 | 전이 조건 | 행동 |
|---|---|---|
| `patrol` | 초기 상태 | 랜덤 목표로 BFS 이동 (1.2초마다 재계산) |
| `chase` | 시야(8칸 LOS) 또는 청각(4칸, 달릴 때만) 감지 | BFS 추적, 0.45초마다 재계산, 속도 증가 |
| `search` | 시야 상실 | 마지막 목격 지점 이동 후 반경 3칸 배회 (8~12초) |

레벨당 마리 수 `min(level, 4)`, 레벨마다 속도 +12%. 접촉 시 즉사(`MON_KILL_R = 0.7`).

**발각 순간** — `onSpotted()`가 `playShriek()`(높고 날카로운 비명)을 울리고
`CHARGE_TIME`(1.5초) 동안 속도에 `CHARGE_BOOST`(×1.35)를 곱해 달려듭니다.
추격이 이어지는 동안에는 `SNARL_MIN`~`+SNARL_VAR` 간격으로 `playGrowl()`을 반복합니다
(18칸 밖은 소리가 뭉치므로 생략).

> `CHARGE_TIME` / `CHARGE_BOOST` / `SNARL_*`는 **난이도와 체감에 직결되는 값**입니다.
> 사용자가 직접 플레이하며 조정할 값이므로 임의로 올리지 마십시오.

### 몬스터 외형 — 관절 리그

몸통·머리·팔(2단)·다리를 `THREE.Group` 회전축에 매단 구조입니다.
`jointLimb()`이 축 Group을 만들고 메시를 길이의 절반만큼 내려 답니다 —
이렇게 해야 Group을 돌렸을 때 **메시 중앙이 아니라 관절을 중심으로** 회전합니다.

지오메트리·재질은 모듈 레벨에서 1회만 만들어 모든 몬스터가 공유합니다.

### 걸음걸이 — 위상은 "시간"이 아니라 "간 거리"로 돈다

**이게 이 리그에서 가장 중요한 규칙입니다.** `anim += dt * 상수`로 돌리면 다리가 도는
속도와 실제 이동 속도가 따로 놀아, 발이 땅을 딛지 않고 허공을 젓습니다(footskate).
눈에는 "속도에 비해 팔다리를 촐싹댄다"로 읽힙니다.

```js
// 금지 — 실제 이동거리와 무관
this.anim += dt * (chasing ? 9.5 : 3.6);

// 올바름 — 한 걸음 = 한 보폭이 항상 성립
const stride = 2 * MON_LEG_LEN * Math.sin(legAmp);
this.anim += (이번_프레임_이동거리 / stride) * Math.PI;
```

실측 (레벨 1):

| 상태 | 실제 속도 | 보폭 | 걸음수 | 다리가 주장하는 속도 | 어긋남 |
|---|---|---|---|---|---|
| 시간 기반 · 순찰 | 1.26 m/s | 0.52 m | 1.15/s | 0.59 m/s | 0.47배 |
| 시간 기반 · 추격 | 2.36 m/s | 1.51 m | 3.02/s | 4.57 m/s | **1.93배** |
| **거리 기반 · 순찰** | 1.26 m/s | 0.87 m | 1.43/s | 1.24 m/s | **0.99배** |
| **거리 기반 · 추격** | 2.34 m/s | 1.87 m | 1.26/s | 2.35 m/s | **1.00배** |

거리 기반이면 돌진 보정(×1.35)과 레벨당 속도 증가(+12%)까지 **전부 자동으로** 맞습니다.

**"성큼성큼"은 보폭을 키워서 만듭니다.** `MON_STRIDE_CHASE`를 0.62 → 0.80rad으로 올리면
보폭이 1.87m가 되고, 같은 속도를 내는 데 걸음수가 3.02 → 1.26회/s로 **2.4배 줄어듭니다.**
추격이 순찰보다 걸음수가 적어지는데, 서두르지 않는데 빨리 다가오는 게 되어 더 무섭습니다.

**몸통은 "위가 넓고 아래가 좁다"여야 합니다.** 반대로 두면(윗반지름 .115 / 아랫반지름 .165)
어깨 축이 몸통 실루엣 안에 파묻혀, 팔이 몸통이 끝나는 **허리 높이에서야** 밖으로 나옵니다.
그러면 어깨가 한참 아래에 붙은 것처럼 보입니다. `MON_SHO_ARM_X`도 `MON_TORSO_RT`보다
커야 합니다.

**팔은 다리와 약분되는 주파수만 쓰십시오.** 예전에는 팔 옆벌림이 1.73배, 팔꿈치가 2.4배라
영원히 같은 자세로 돌아오지 않았고, 그게 진동으로 읽혔습니다. 지금은 팔이 다리와 같은
주파수의 반대 위상이고, 보조 흔들림만 0.5배입니다.

**구현 시 함정 두 가지:**
- 멈추면 위상이 얼어붙어 정지 화면이 됩니다 → 속도 0 부근에서만 아주 작은 시간 기반
  숨쉬기를 섞습니다(`dPhase < dt*0.5`이면 바닥값).
- 레벨 시작·순간이동으로 좌표가 점프하면 위상이 폭주합니다 → 프레임당 상한(`dt*14`).

**발소리도 같은 위상에서 뽑습니다** — `anim`이 π를 넘는 순간이 발이 닿는 순간입니다.
예전에는 `dt*spd*1.5`라는 별도 계산을 써서 눈에 보이는 걸음(3.02/s)과 소리(1.13/s)가
**2.7배 어긋나** 있었습니다.

**임팩트는 실루엣이 아니라 움직임에서 나옵니다.** 정적인 형태만 다듬는 것보다
걸음걸이를 맞추는 쪽이 훨씬 효과가 큽니다.

### 입 — 두개골이 갈라진다

`this.head` 안에 `upperJaw` / `jaw` 두 Group을 형제로 넣고, 경첩을 머리 **뒤쪽**(z=-0.10)에
둬서 앞이 벌어지게 합니다. `chase`에 들어가면 열리고 시야를 잃으면 닫히며, 즉시 여닫지 않고
보간합니다(여는 쪽이 빠름 11, 닫는 쪽이 느림 4).

- 이빨은 원뿔 12개를 **`mergeParts()`로 미리 한 덩어리**로 합쳐 턱마다 메시 1개입니다.
  ⚠ `mergeParts`/`part`가 쓰는 `_mgMat` 등이 몬스터 지오메트리보다 **아래에서** `const`로
  선언되므로, 모듈 평가 시점에 호출하면 TDZ에 걸립니다. 첫 생성 때 만들어 캐시합니다.
- **위턱은 아주 조금만 젖히십시오**(`jawOpen * 0.15`). 0.34로 뒀더니 두개골이 통째로 들려
  **머리가 몸에서 떨어져 나온 것처럼** 보였습니다. 벌리는 일은 아래턱이 합니다.
- 반구는 뚫려 있어 벌리면 속이 비칩니다. 갈라진 면을 `mMonMaw` 원판으로 막습니다.
  ⚠ 원판의 법선은 `+Z`이고 X축으로 θ 돌리면 `(0,-sinθ,cosθ)`를 향합니다. 위턱의 열린 면은
  아래를 보므로 `+π/2`, 아래턱은 위를 보므로 `-π/2`입니다. **부호를 반대로 뒀더니 두 원판이
  모두 바깥을 향해 백페이스로 컬링되어, 머리 속이 뚫린 채 천장이 그대로 비쳤습니다.**
- **목(`gMonNeck`)이 반드시 있어야 합니다.** 아래턱이 벌어지면 머리 아래가 통째로 비어
  천장이 보입니다. 목은 `head`가 아니라 `group`의 자식이어야 고개를 돌려도 따라 돌지 않습니다.
- 이빨은 **조명을 받는 재질**입니다(`mkMat`). 몸통처럼 빛을 무시하게 만들면 어두운 구역에서
  이빨만 둥둥 떠 보여 검은 실루엣이 어둠에 묻히는 설계가 통째로 깨집니다.

**빌보드 평면은 쓰지 마십시오.** 한때 캔버스로 그린 전신 텍스처를 평면 1장에 붙여
카메라 쪽으로 돌렸는데, 팔을 휘저을 수 없고 항상 플레이어를 쳐다봐서 진행 방향 단서가
사라집니다. (`THREE.Sprite`는 더 나쁩니다 — r128의 `SpriteMaterial`은 광원의 영향을
아예 받지 않습니다.)

회전은 `this.facing`(진행 방향)으로 목표를 두고 매 프레임 보간합니다.
각도차를 `-π~π`로 감지 않으면 경계에서 한 바퀴 헛돕니다.

### 왼팔만 비정상적으로 길다 — 좌우 비대칭

검은 실루엣은 표면 디테일이 전혀 안 보입니다(3.2칸에서 화면의 2.6%). 그래서 "더
끔찍하게"는 **윤곽과 움직임**으로만 가능하고, 그중 가장 직접적인 것이 좌우 비대칭입니다
— 사람의 팔다리 개수·비율을 깨는 것이 가장 빠르게 "사람이 아니다"를 전달합니다.

`MON_ARM_LONG_UP`/`MON_ARM_LONG_FORE`(0.90 / 1.00)로 왼팔만 별도 지오메트리
(`gMonUpperL`/`gMonForeL`)를 씁니다. 편 채로 늘어뜨리면 손끝이 바닥에 거의 닿습니다
(어깨 1.82 − 1.90 ≈ 바닥). 오른팔(`gMonUpper`/`gMonFore`, 0.66/0.74)은 그대로 둡니다.

**애니메이션 코드는 손대지 않았습니다.** `armL`/`foreL`에 걸리는 회전식은 오른팔과
동일한 `sw` 기반 진자 운동이고, 긴 지오메트리가 같은 각도로 흔들리는 것만으로 손끝이
훨씬 아래·바깥까지 쓸고 다닙니다. 검증 시 주의할 점 — **정지한 정면 포즈에서는 안
보입니다.** 어깨 부착 지점이 몸통 윗반지름(`MON_TORSO_RT=.175`)과 겨우 0.01 차이라,
팔이 거의 몸통에 붙어 있는 각도(rest, 대략 13°)에서는 정면에서 몸통 실루엣에 가려
길이 차이가 드러나지 않습니다. **실제 걸음(순찰·추격) 애니메이션이 걸린 상태에서,
뒷모습이나 3/4 각도로 봐야** 다리보다 길게 늘어져 끌리는 왼팔이 보입니다.

### 몬스터 재질 — `MeshBasicMaterial` 순수 검정

**`mkMat()`을 쓰지 않는 유일한 예외입니다.** `mkMat`은 `roughness`/`metalness`를
셰이딩 파라미터로 옮기는 팩토리인데, 여기서는 셰이딩 자체가 없어야 하므로 옮길 것이 없습니다.

`MeshBasicMaterial`은 조명 계산을 통째로 건너뜁니다. 그래서

- 어떤 밝기에서도 **순수한 검정 실루엣이 보장**됩니다
- Phong/Standard보다 **쌉니다**
- 눈 발광이 필요 없어져 `monLightPool`을 통째로 삭제했습니다 (광원 5/6/9/11 → **4/5/6/8**)

`fog`는 그대로 적용됩니다. 멀어질수록 안개색에 잠겨 대비가 약해지는데, 이게 거리감을
만들고 참고 사진의 원경 실루엣과도 맞습니다.

### 밝기에 따른 몬스터 가시성 (실측)

검은 실루엣은 배경 밝기에 따라 완전히 다르게 보입니다. 이건 버그가 아니라 **설계**입니다.

거리 3.2칸 고정, 입을 벌린 상태(이빨 노출), 아래 네 가지를 모두 통제한 값입니다.

| 구역 | 배경 밝기 | 몬스터가 차지하는 뚜렷한 화소 |
|---|---|---|
| 밝은 복도 | 121 | **2.62%** |
| 중간 | 63 | 1.50% |
| 어두운 구역 | 24 | **0.71%** |

어두운 구역에서는 사실상 보이지 않습니다. 밝은 곳에서는 멀리서부터 보여 압박을 주고,
어두운 곳에서는 **발소리·으르렁·심장박동만** 남습니다.
**입을 벌려 이빨이 드러난 상태로 재도 어두운 구역 수치가 오르지 않습니다** — 이빨을
조명받는 재질로 둔 이유가 이것입니다.

> **몬스터 가시성을 실측할 때 반드시 통제할 것 네 가지.** 네 번 다 틀린 값을 얻었고,
> 그중 두 번은 "어두운 곳에서 더 잘 보인다"는 물리적으로 불가능한 결과였습니다.
> 1. **`#danger` 비네트를 숨길 것** — 몬스터가 가까우면 화면 전체가 붉게 물들어
>    "몬스터 있음/없음" 두 프레임의 차이를 통째로 오염시킵니다.
> 2. **몬스터 속도를 0으로 고정할 것** — 안 그러면 AI가 대기 시간 동안 카메라 쪽으로
>    걸어와, 어두운 구역에서 오히려 화면을 가득 채웁니다(측정값이 뒤집힘).
> 3. **두 프레임의 차이가 `group.visible` 하나뿐이게 할 것** — 위치·상태를 양쪽에서
>    똑같이 세팅하십시오.
> 4. **고장 난 천장등의 스터터를 끌 것** — 밝기가 ×0.18로 떨어지는 순간이 두 스크린샷
>    사이에 걸리면 화면 전체가 바뀌어 몬스터 차이를 삼켜 버립니다. 어두운 구역일수록
>    상대 변화가 커서 더 심하게 오염됩니다. 측정용 HTML에서 조건을 `false`로 바꾸십시오.
>
> **거리도 반드시 기록하십시오.** 화소 비율은 거리의 제곱에 반비례합니다 —
> 이 표는 3.2칸 기준이고, 거리를 명시하지 않은 예전 측정값(밝은 복도 32%)은
> 몬스터가 훨씬 가까이 있던 상태라 이 표와 직접 비교할 수 없습니다.

### 크롤러 좀비 — 두 번째 몬스터 타입

`MONSTER_TYPES={standard,crawler}` config 객체로 감각·속도를, `Monster` 생성자의
`if(this.type==='crawler')` 분기로 메시를 나눈다. **재질(`mMonBody`)·머리·입·이빨
시스템(`mkJaw`/`toothRows`/`gMonSkull*`)은 두 타입이 완전히 공유** — 새 재질도
새 광원도 하나 없다. 최우선 규칙 1번(광원 개수 고정)이 몬스터 타입을 늘릴 때도
그대로 적용된 사례다.

**감각**: `seesPlayer()`/`hearsPlayer()`가 모듈 전역 상수 대신 `this.cfg`를 참조한다.
크롤러는 시야 5칸(standard 8칸)로 좁은 대신, 청각 반경 6.5칸(standard 4칸)에
`hearMinSpd=0`이라 **걸어도 들린다**(standard는 `P.spd>=1.5`, 즉 뛸 때만). 속도는
`spdMul=1.28`로 스폰 시점에 접어 넣는다 — `update()`의 속도식(레벨 스케일링·돌진
배율과 얽혀 있음)은 손대지 않고 그대로 둔다.

**메시 — 절지동물(거미) 다리.** 처음엔 서 있는 개체를 눕힌 낮은 네발 자세로
설계했는데, 사용자 피드백으로 다시 짰다: 배가 바닥(로컬 -0.10)에 거의 붙고,
다리 4개(팔 2 + 다리 2)가 전부 **관절 2개** 구조다 — 관절1(위 마디)이 몸통 옆에서
위·바깥으로 135°(`CRAWL_UPPER_SPLAY`) 뻗고, 관절2(아래 마디)가 무릎에서 다시 거의
수직으로(`CRAWL_LOWER_WORLD`≈11°) 꺾여 바닥까지 내려온다. `CRAWL_LOWER_LOCAL`은
"관절2의 부모(관절1) 기준 상대 회전"이다 — 관절1의 회전을 상쇄하고 원하는 절대각을
만들도록 `LOWER_WORLD - UPPER_SPLAY`로 역산했다. 다리 4개를 로컬 헬퍼
`mkSpiderLeg(sx,z)`로 만든다(`sx`=좌우 부호, `z`=앞/뒤 앵커) — `jointLimb()` 자체가
이미 이런 헬퍼 패턴이라 4번 반복을 줄이는 정도는 과도한 추상화가 아니라고 판단했다.

이전 설계(뒷다리만 관절 1개, 서서 다리를 늘어뜨린 자세)의 흔적인 `gCrawlArmU`/
`gCrawlArmF`/`gCrawlLeg`/`CRAWL_ARM_UPPER`/`CRAWL_ARM_FORE`/`CRAWL_LEG_LEN`은
전부 제거했다. 지금은 다리 4개가 전부 같은 지오메트리(`gCrawlLegU`/`gCrawlLegF`)를
쓴다.

**애니메이션 — 대각선 보행.** 대각선 두 다리가 짝을 이뤄 반대 위상으로 움직인다
(앞왼쪽`armL`+뒤오른쪽`legR` / 앞오른쪽`armR`+뒤왼쪽`legL`). 관절1의 splay/꺾임
(생성자에서 정한 `rotation.z`)은 정적인 다리 **형태**라 `update()`가 건드리지
않는다 — 걷는 움직임은 `rotation.x`(앞뒤 스윙)만으로 표현한다. standard의 팔꿈치가
"거의 고정 + 살짝 지연"인 것과 같은 원리로, 관절2(아래 마디)는 관절1의 절반 진폭
(`lowerAmp=speedFrac*0.30` vs `upperAmp=speedFrac*0.6`)으로 따라간다.

진폭은 **이동 중일 때만, 실제 속도(`spd`)에 비례**해서 커진다
(`speedFrac=moved>1e-4?Math.min(1,spd/(MON_BASE_SPD*2.5)):0`) — 정지하면 완전히 0이라
standard처럼 정지 시 미세하게 숨 쉬는 처리가 없다(다리가 이미 정적인 splay
자세이므로 굳이 필요 없었다). 걸을 때 상하로 튀는 반동(`bobAmt`)도 같은 `speedFrac`을
써서 정지 시 사라진다.

> ⚠️ **`update()`의 사지 애니메이션 블록을 `if(this.type==='crawler')`로 분기했다.**
> standard 경로는 원래 코드를 한 글자도 안 바꾸고 `else` 안으로 그대로 옮겼다 —
> `bobAmt`라는 공유 지역변수 하나만 새로 도입해 두 분기 다음에 오는
> `this.group.position.set(...)` 한 줄을 공유한다.

**검증 시 겪은 테스트 버그** (게임 코드 문제 아님, 기록해 둔다): 크롤러를 강제로
생성해 실제 게임 루프에 붙이는 검증 스크립트에서, `let monsters=[]`가
`startLevel()`에서 **재할당**(`monsters=[]`, 뮤테이션이 아니라 새 배열 대입)되는
줄을 모른 채 `window.__monsters=monsters`로 참조를 캡처했다. `#startBtn` 클릭
**이전**에 캡처한 그 참조는 클릭 후 재할당된 진짜 배열과 다른 객체가 되어, 거기 넣은
크롤러가 실제 `animate()` 루프의 `for(const m of monsters)`에서 빠져 **한 번도
update()가 안 불렸다.** 화면엔 정지 자세로 보여서 렌더링은 맞는데 움직임만 없어
보이는, 원인 파악이 까다로운 종류의 버그였다. `()=>monsters` 같은 getter로 캡처해야
재할당 후에도 최신 값을 본다 — 이 프로젝트의 다른 재할당 상태(`mazeGrid`, `exitPos`,
`arrowPos`, `keyPos` 등)를 검증 스크립트에서 참조할 때도 항상 이 패턴을 쓴다.

**스폰**: `startLevel()`에서 레벨 3부터 스폰 슬롯 중 **인덱스 0 하나만** 크롤러로
교체한다(`MON_COUNT(lv)` 공식·총 마릿수는 그대로, 타입 구성만 바뀐다). 레벨 1~6에서
실측한 구성:

| 레벨 | 총 마릿수 | 타입 구성 |
|---|---|---|
| 1 | 1 | standard |
| 2 | 2 | standard × 2 |
| 3 | 3 | **crawler** × 1, standard × 2 |
| 4~6 | 4 | **crawler** × 1, standard × 3 |

계측 패널 LIGHT 수는 프리셋별로 5/6/7/9 그대로다 — 크롤러가 `mMonBody`만 쓰고
새 광원을 만들지 않기 때문이다.

### 최종 접근 — 격자 이동의 사각을 메우는 코드

**이 코드를 제거하면 눈앞에 두고도 죽지 않는 버그가 되살아납니다.**

BFS는 칸 중심에서 칸 중심으로만 이동합니다. 몬스터가 플레이어의 칸에 도착하면
`bfsNext()`가 (시작==목표) `null`을 반환해 **거기서 멈춥니다.** 그런데 플레이어는
칸 안 아무 데나 설 수 있어(벽 충돌 여유 `PLAYER_R=0.32` 때문에 칸 중심에서
최대 **0.96칸**) 즉사 판정 `MON_KILL_R=0.7`에 닿지 않는 사각이 생깁니다.

실측 재현: 플레이어를 칸 구석(중심에서 0.93칸)에 두고 몬스터를 같은 칸 중심에
세우면, 추격 상태인데도 **거리가 0.933에서 5초간 멈춘 채 죽지 않았습니다.**

그래서 `chase` + 시야 확보 + 거리 `CELL*1.2` 이내이면 격자를 버리고 플레이어의
**실제 좌표**로 직접 향합니다. 시야 조건이 있어 벽을 뚫지 않습니다
(추격 349프레임 동안 몬스터가 벽 칸에 들어간 경우 0회).

직접 접근 중에도 `gx`/`gz`를 따라 갱신하되 **길 칸일 때만** 옮깁니다.
벽 칸에서 `bfsNext()`가 출발하면 경로를 찾지 못합니다.

**과거 크래시 이력**: `searchPos`가 `null`인 채 `search` 상태에 진입해
`this.searchPos[0]` 접근에서 `TypeError` 발생. 현재는 `chase` 진입/유지 중
매 프레임 `searchPos`를 갱신하고, `search` 로직에서도 `null` 검사 후 `patrol` 복귀하도록 이중 방어됨.
**이 방어 코드를 제거하지 마십시오.**

`bfsNext()`도 NaN 유입과 무한 역추적을 막는 가드가 들어 있습니다. 동일하게 유지 필요.

### 공간 오디오 (Web Audio API)
`PannerNode` + HRTF 패닝 사용. 리스너는 매 프레임 카메라에 동기화(`updateAudioListener()`).

| 소리 | 방식 | 함수 |
|---|---|---|
| 플레이어 발소리 | 2D | `footstep()` |
| 몬스터 발소리 | **3D 위치** | `monsterStep(x, z)` |
| 발각 비명 | **3D 위치**, 녹음 음원 | `playShriek(x, z)` |
| 추격 중 으르렁 | **3D 위치** | `playGrowl(x, z)` |
| 형광등 버즈 | **3D 위치** | `panelBuzz(x, y, z)` |
| 심장박동 | 2D, 거리 비례 | `heartbeat(intensity)` |
| 열쇠 획득 | 2D | `playKeyPickup()` |
| 잠긴 문 | 2D | `playLocked()` |

### 거리감 — 멀면 작게, 가까우면 크게

3D 소리는 `PannerNode`의 `inverse` 거리 모델을 씁니다. **`rolloffFactor`가 거리감을
좌우합니다** — 값이 작으면 멀리서도 또렷이 들려 위치 판단이 안 됩니다.
현재 비명 1.9 / 으르렁 1.8 / 발소리 1.7이고, `refDistance`는 2 안팎입니다.

심장박동은 2D라 거리 모델이 없어 게인을 직접 계산합니다. **선형이 아니라 제곱**
(`.26 × intensity²`)입니다. 선형이면 6칸 거리에서도 또렷이 들려 긴장감이 평평해집니다.

출구의 저주파 험은 **의도적으로 제거**했습니다. 되살리지 마십시오.
그 대가로 출구를 소리로 찾을 수 없어졌고, 미니맵이 유일한 단서입니다.

### 발각 비명만 녹음 음원 (`SHRIEK_MP3`)

유일한 외부 음원입니다. **base64 data URI로 index.html에 심어** 뒀습니다 —
단일 HTML 파일 구성이 유지되고, `sw.js` 캐시 목록·오프라인 동작·미리보기 빌드가
모두 손댈 필요 없이 그대로 굴러갑니다. mono 44.1kHz MP3 2종(1.45초 + 1.26초), 21KB.

**동시 재생 금지** — 몬스터가 최대 3마리라 각자 발각되면 비명이 겹쳐 뭉갭니다.
`_shriekBusyUntil`이 재생 종료 + `SHRIEK_GAP`까지 새 비명을 막습니다.
클립은 매번 무작위로 고르고 `playbackRate`도 ±6% 흔듭니다.

**MP3인 이유**: 원본은 AAC(m4a)였는데 iOS Safari는 되지만 오픈소스 Chromium 빌드에는
AAC 디코더가 없어 검증이 불가능합니다. MP3는 전 브라우저가 지원합니다.

`decodeAudioData`가 실패하면 `playShriekSynth()`(톱니 2개 합성)로 자동 폴백합니다.
**이 폴백을 제거하지 마십시오** — 코덱 지원은 브라우저마다 다릅니다.

디코드는 `unlockAudio()` 안에서 1회만 합니다. `AudioContext`가 사용자 제스처
안에서만 열리기 때문입니다.

> 음원을 교체할 때는 **반드시 앞뒤 무음을 잘라내고 피크를 1.0 아래로 내리십시오.**
> 원본은 4.3초에 비명이 두 번 들어 있고 피크가 1.329로 클리핑돼 있었습니다.
> ffmpeg에서 `afade`는 반드시 `atrim,asetpts=PTS-STARTPTS` **뒤에** 두십시오.
> 앞에 두면 자르기 전 원본 타임라인 기준으로 페이드가 걸려 소리 중간이 잘립니다.

**iOS 주의**: `AudioContext`는 사용자 제스처 안에서 `resume()`해야 소리가 납니다.
시작/재시도/다음레벨 버튼에 `unlockAudio()`가 연결되어 있습니다.

구형 Safari 대응으로 `positionX` (AudioParam) / `setPosition()` 양쪽 폴백이 있습니다. 유지하십시오.

### Fog of War 미니맵
`visited[][]` 2차원 배열에 탐험 여부 기록.
전방 **2칸 · 시야각 120°(±60°)** 안에서 LOS가 통하는 칸만 밝힙니다.
몬스터는 해당 칸이 탐험된 경우에만 미니맵에 표시됩니다.

### 조작 — 좌측 이동 / 우측 시선

모바일 1인칭 게임의 표준 배치입니다. 화면을 좌우 절반으로 나눈 **보이지 않는 터치 영역** 2개.

| 영역 | 역할 |
|---|---|
| `#touchL` (좌측 절반) | 플로팅 아날로그 스틱 — 누른 자리에 생기고 떼면 사라짐 |
| `#touchR` (우측 절반) | 좌우=`P.yaw`, 상하=`P.pitch` 문질러 시선 회전 (감도 둘 다 `0.011`) |
| 키보드 | WASD / 화살표 (상하 시야는 키보드에 없음 — 터치 전용) |

### 상하 시야 — `P.pitch`

`camera.rotation.order='YXZ'`가 이미 지정돼 있어 pitch를 넣는 작업이 값 하나 추가로
끝났습니다. YXZ 순서가 아니면 고개를 든 채 좌우를 볼 때 화면이 기웁니다.

`PITCH_LIMIT`(≈60°)로 클램프합니다 — 없으면 시야가 뒤집힙니다. **이동·미니맵 시야·3D
오디오는 전부 `P.yaw`만 씁니다.** pitch는 카메라 렌더링에만 관여하고 게임플레이 판정에는
관여하지 않습니다 — 위를 봐도 걷는 방향은 그대로, 하늘을 날지 않습니다.

레벨 시작 시 `P.pitch=0`으로 리셋합니다(`P.yaw`와 동일한 자리). 안 하면 이전 레벨에서
위·아래를 보던 상태로 새 레벨이 시작됩니다.

**`jFwd`는 전진이 -1**입니다(게임 루프에서 `-jFwd`로 반전). `dSide`는 우횡이동이 +1.
스틱은 이 둘에 **소수값**을 넣습니다 — 이동 계산식이 그대로 비례 속도를 만들어 주므로
계산식은 손댈 필요가 없습니다.

**살살 밀면 잠입이 됩니다.** 몬스터의 청각 감지는 `P.spd >= 1.5`가 조건인데,
스틱을 1/3 이하로 밀면 속도가 그 아래로 유지됩니다(12px → 0.59).

**원형 클램프를 제거하지 마십시오.** 없으면 대각선 입력이 `√2`배 빨라집니다.
십자키 시절 전진+횡이동을 동시에 누르면 4.50이 아니라 **6.36**이 나오던 버그가
이 클램프로 해결됐습니다.

`touchcancel`도 반드시 `touchend`와 같이 처리해야 합니다 — 놓치면 손을 뗐는데
계속 걸어갑니다.

**⚠️ `#touchL`은 성능 패널 아래에서 시작해야 합니다.** `#perfToggle`(z-index 31)이
좌측 상단을 차지하고 있어, 영역이 겹치면 스틱 대신 패널이 토글됩니다.
세로는 `top:272px`, 가로는 `top:178px`(미디어쿼리)로 맞춰 뒀습니다.

### 가로모드

`@media (max-height:520px)`로 처리합니다. 세로 기준으로 짜인 고정 크기(성능 패널,
미니맵 110px, HUD)가 390px 높이를 다 잡아먹기 때문입니다.

> **과거 버그**: `#controls`의 z-index가 `#perfToggle`(31)보다 낮아, 화면이 짧은
> 가로모드에서 성능 패널의 투명 터치판이 십자키를 덮어 **버튼 입력이 통째로 먹히지
> 않았습니다.** `elementFromPoint`로 확인하면 전진 버튼 자리에서 `perfToggle`이
> 잡혔습니다. 조작 UI를 추가할 때는 항상 두 방향 모두에서 히트테스트를 확인하십시오.

### 노치 — `viewport-fit=cover` + 안전 영역 여백

**`viewport-fit=cover`를 빼지 마십시오.** 이게 없으면 노치가 있는 아이폰에서 iOS가
**뷰포트를 안전 영역 안으로 줄여버리고**, 남는 자리에 배경색(`#080a10`)이 그대로
드러납니다. 가로모드에서 위·좌·우에 검은 띠가 생기고 HUD가 잘렸습니다.
`apple-mobile-web-app-capable`이 켜져 있어 상단까지 생깁니다.

화면은 끝까지 그리되, UI만 `:root`의 `--sat/--sal/--sar/--sab` 변수로 안쪽에 둡니다.
`env(safe-area-inset-*, 0px)`이라 미지원 브라우저에서는 0으로 떨어집니다.

터치 영역(`#touchL`/`#touchR`)은 일부러 화면 끝까지 둡니다 — 노치 안쪽은 애초에
손가락이 닿지 않으므로 여백을 줄 이유가 없고, 영역은 넓을수록 좋습니다.

> 헤드리스 브라우저에는 `env()` 값이 없어 **실기기 확인이 필요합니다.**
> 검증할 때는 `:root` 변수를 직접 덮어써 노치를 흉내낼 수 있습니다.

### 성능 계측 오버레이
좌상단에 FPS / 최저 / 평균 / DRAW / LIGHT / TRIS / OBJ / 품질 표시.
- 1회 탭 = 기록 초기화 (+ `renderScale`을 프리셋 기본값으로 복원)
- 3회 탭 = 표시·숨김

집계와 적응형 조절은 **패널을 숨겨도 계속 동작**합니다. `perfOn`은 DOM 갱신만 건너뜁니다.

`renderer.info.render`는 `render()` 시작 시 리셋되므로 **반드시 렌더 직후**에 읽어야 합니다.
FPS 계측에는 클램프 전 `rawDt`를 사용합니다(클램프된 `dt`는 저프레임을 실제보다 좋게 보이게 함).

### 에러 방어
- 전역 `error` / `unhandledrejection` 핸들러 → 콘솔에 상세 출력
- `animate()` 본문 전체가 `try-catch` → 예외가 나도 렌더 루프가 죽지 않음

> CDN 모듈은 크로스오리진이라 예외 시 브라우저가 `Script error.`로만 표시합니다.
> 위 핸들러가 없으면 원인 파악이 불가능하므로 제거하지 마십시오.

---

## 품질 설정 시스템 (구현 완료)

`QUALITY_PRESETS` — CONSTANTS 바로 뒤, THREE 셋업 이전에 정의됩니다.
텍스처·재질·renderer 생성이 모두 `Q`를 참조하므로 **위치를 뒤로 옮기면 안 됩니다.**

| 파라미터 | LOW | MEDIUM | HIGH | ULTRA |
|---|---|---|---|---|
| pixelRatio 상한 (`prCap`) | 1.0 | 1.5 | 2.0 | 3.0 |
| `renderScale` 기본값 | 0.6 | 0.8 | 1.0 | 1.0 |
| `panelLights` | 2 | 3 | 4 | 6 |
| **총 광원 수** (ambient + 천장등 + 출구 + 손전등) | **5** | **6** | **7** | **9** |
| `texSize` | 128 | 256 | 256 | 512 |
| `aniso` (0=최대) | 1 | 1 | 4 | 0 |
| `aa` | ✗ | ✗ | ✓ | ✓ |
| `matType` | phong | phong | standard | standard |
| `ambient` | **0.78 (전 프리셋 동일)** | ← | ← | ← |
| `panelRange` | **7.2 (전 프리셋 동일)** | ← | ← | ← |
| `panelPower` | 1.42 | 1.15 | 1.00 | 1.00 |
| `panelInterval` | 0.20s | 0.15s | 0.12s | 0.10s |
| `mmInterval` | 8f | 6f | 4f | 4f |
| `panning` | equalpower | HRTF | HRTF | HRTF |
| `targetFps` (적응형) | 30 | 55 | 55 | 60 |

### 설계 원칙 3가지

**1. 프리셋은 부팅 시점 상수다.**
`antialias`는 WebGL 컨텍스트 생성 파라미터라 런타임 변경이 불가능하고, 텍스처·재질도 전면
재생성이 필요합니다. 그래서 프리셋 UI는 **타이틀 화면에만** 두고, 변경 시 `localStorage`에
저장하고 `location.reload()` 합니다. 타이틀에서는 잃을 진행 상황이 없고 SW 캐시 덕에
리로드가 즉시 끝납니다. dispose/rebuild 경로를 만들지 마십시오 — 코드량 대비 이득이 없습니다.

**2. 런타임에 움직이는 축은 `renderScale` 하나뿐이다.**
`setPixelRatio(min(devicePixelRatio, Q.prCap) × renderScale)` — 해상도만 바꾸므로 셰이더
재컴파일이 없습니다. 적응형 자동 조절(`adaptQuality()`)은 이 값만 건드립니다.
**자동 조절이 광원·재질·텍스처를 만지면 플레이 중 hitching이 발생합니다. 절대 금지.**
하향 3초 / 복구 10초의 히스테리시스로 경계 진동을 막습니다. 하한 `ADAPT_MIN = 0.5`.

**3. 게임 밸런스 값은 프리셋에 넣지 않는다.**
`fog` 밀도, `MON_SIGHT_R`, `MON_HEAR_R`, 몬스터 속도는 품질 설정으로 바뀌면 안 됩니다.
품질에 따라 난이도가 달라지는 것은 버그입니다.

### 재질 — Lambert가 아니라 Phong인 이유

r128의 `MeshLambertMaterial`은 **정점 단위(Gouraud) 조명**입니다.
벽이 세그먼트 1개짜리 `BoxGeometry`(정점 8개)라, 천장등 `PointLight`의 감쇠가 정점에서만
계산되어 빛이 사각 얼룩으로 뭉개집니다. 이 게임은 천장등이 만드는 빛웅덩이가 분위기의
핵심이라 치명적입니다.
`MeshPhongMaterial`은 픽셀 단위 조명을 유지하면서 Standard의 PBR 연산(BRDF)만 걷어냅니다.

재질은 `mkMat()` 팩토리를 통해서만 생성하십시오. `roughness`/`metalness`를
`shininess`/`specular`로 근사해 넘깁니다. **`new THREE.MeshStandardMaterial()` 직접 호출 금지.**

### 텍스처 — 크기 파라미터화 필수

`makeWallpaper/makeCarpet/makeDropCeiling(size)`의 모든 좌표·크기·반복 횟수는 **256px 기준으로 작성한 뒤
`s = size/256`을 곱합니다.** 캔버스 크기만 바꾸면 128에서는 벽돌이 2×4개만 나오고,
바닥 타일 좌표(256까지 채움)가 캔버스 밖으로 넘어가 타일링 이음새에 빈 영역이 드러납니다.
텍스처에 무언가 추가할 때 이 규칙을 지키십시오. 마무리는 `finishTex()`가 담당합니다(wrap + anisotropy).

### 밝기 보정 — 광원을 늘리지 않고 조절하는 축

광원이 적은 프리셋은 천장등 사이 구간이 어두워집니다. 이때 `panelLights`를 늘리면
1번 규칙에 정면으로 위배되므로, 대신 아래 세 값을 씁니다. **셋 다 픽셀당 광원 순회
횟수를 바꾸지 않아 사실상 공짜입니다.**

| 값 | 무엇을 정하나 | 프리셋별로 달라도 되나 |
|---|---|---|
| `ambient` | **어두운 구역의 밝기** (천장등이 없으니 이것뿐) | **안 됨 — 전 프리셋 동일** |
| `panelRange` | 감쇠 "모양" (`PointLight.distance`) | **안 됨 — 전 프리셋 동일** |
| `panelPower` | 천장등 세기 배수 | 됨. 광원 개수 차이를 여기서 보정 |

**앞의 둘을 프리셋별로 다르게 두면 안 되는 이유**는 백룸에서 어두운 구역이 장식이 아니라
**게임플레이**이기 때문입니다 — 검은 몬스터가 거기서 보이지 않습니다. 품질 설정이
어두운 구역의 밝기를 바꾸면 곧 난이도를 바꾸는 것이고, 설계 원칙 3번 위반입니다.

실측으로 두 번 확인했습니다.

| 시도 | 어두운 구역 (LOW / HIGH) | 결과 |
|---|---|---|
| `ambient` 1.18 / 1.10 | 90 / 75 | 명암비가 1.35배 대 1.83배로 갈림 |
| `panelRange` 8.8 / 7.2 | 복도 중간 88 / 46 | 넓은 range가 벽을 뚫고 어두운 구역까지 밝힘 |
| **현재 (둘 다 통일)** | **29.5 / 30.4** | 프리셋 간 편차 소멸 |

> ⚠️ **`shadowMap`이 꺼져 있어 `PointLight`는 벽을 그대로 통과합니다.**
> 그래서 `panelRange`(7.2 = 3.6칸)가 `DARK_BLOCK`(5칸)과 비슷하면 어두운 구역 가장자리가
> 이웃 구역 빛으로 밝아집니다. 어두운 구역 크기를 줄이려면 range부터 같이 줄여야 합니다.

**밝기 실측 기준값** (시드 고정 미로, 화면 평균 휘도):

| 위치 | 값 |
|---|---|
| 천장등 바로 아래 | 144~151 |
| 복도 중간 | 40~43 |
| 어두운 구역 깊은 곳 | **29~31 (프리셋 무관)** |

과거 던전에서 `ambient 4.6 / torchRange 10 / torchPower 1.3`을 한꺼번에 올렸다가
평균 밝기가 34 → 69로 2배가 되어 분위기가 날아간 적이 있습니다. **밝기는 반드시
한 번에 하나씩 바꾸고 매번 실측하십시오.**

### 자동 감지

`detectQuality()` — `localStorage`에 저장값이 없을 때만 동작합니다.
모바일은 `hardwareConcurrency ≤ 2 || deviceMemory ≤ 2`이면 LOW, 아니면 MEDIUM.
데스크톱은 `≤ 4`이면 MEDIUM, 아니면 HIGH.

**⚠️ 모바일 임계값을 다시 올리지 마십시오.**
iOS Safari는 `deviceMemory`를 **아예 노출하지 않고**(`undefined`),
`hardwareConcurrency`도 대부분 **4로 보고**합니다. 처음에 모바일 LOW 조건을
`cores ≤ 4`로 뒀더니 최신 아이폰까지 전부 LOW로 떨어져 "너무 어둡다"는 문제가
발생했습니다. `deviceMemory`는 `undefined`와 실제 저사양을 구분해야 하므로
`||` 기본값으로 숫자를 채우지 말고 `mem !== undefined` 검사를 유지하십시오.

### 프리셋을 올린다고 항상 좋아지지 않습니다

적응형은 **FPS만 보고 `renderScale`을 내립니다.** 더 무거운 프리셋을 고르면 그 대가로
해상도가 깎여, 실효 픽셀 밀도가 오히려 낮아질 수 있습니다.

아이폰 실측 예:

| | prCap | × renderScale | **실효 배율** | FPS |
|---|---|---|---|---|
| HIGH | 2.0 | ×1.00 | **2.0** | 59~60 |
| ULTRA | 3.0 | **×0.50** (하한) | **1.5** | 53~60 |

ULTRA는 광원이 11개라 절반 해상도로도 60을 못 지키고, 결과적으로 **HIGH보다 흐리고
불안정**합니다. `품질` 행의 배율이 하한(0.5)에 붙어 있으면 그 프리셋은 그 기기에
과합니다 — 한 단계 내리는 것이 화질·프레임 모두에 이득입니다.

### 검증 방법

계측 오버레이 하단에 `품질 <프리셋> ×<renderScale>` 행이 추가되어 있습니다.
`renderScale`이 프리셋 기본값보다 낮으면 노란색으로 표시됩니다(적응형이 하향한 상태).
**1회 탭(기록 초기화)은 `renderScale`도 프리셋 기본값으로 되돌립니다** — 프리셋별 실측 조건을
맞추기 위한 것입니다. 프리셋을 바꿔가며 측정할 때는 항상 1회 탭 후 시작하십시오.

---

## 진행 저장 — `js/save.js`

`localStorage['de_progress']`에 `{bestLevel, totalEscapes, resumeLevel}`를 남깁니다.

`resumeLevel`이 핵심입니다. 이게 있어야 **승리·사망 화면에서 품질을 바꿔도**(= 리로드)
진행이 날아가지 않습니다. 그래서 품질 선택 UI가 타이틀 외 두 화면에도 붙어 있습니다.

- `markLevel(lv)` — 레벨 진입 시. `resumeLevel` 갱신 + `bestLevel` 최고치 유지
- `markEscape(nextLevel)` — 탈출 시. **`resumeLevel`을 다음 레벨로 올립니다.**
  이걸 빼먹으면 승리 화면에서 리로드했을 때 방금 깬 레벨을 다시 하게 됩니다
- `resetRun()` — "처음부터". 평생 기록은 남기고 `resumeLevel`만 1로

저장 형식이 손상돼도 게임이 죽지 않도록 필드별로 `Number.isFinite` 검사 후 받습니다.

---

## 열쇠와 잠긴 문

**출구 자체가 문입니다.** 미로 중간에 문을 세우지 않습니다 — 그러면 열쇠가 문 뒤에
갇혀 클리어 불가능한 판이 나올 수 있고, 이를 막으려면 배치 때마다 도달 가능성 검사를
돌려야 합니다. 출구를 잠그면 미로 전체가 **언제나** 도달 가능하므로 그 검사가 통째로 사라집니다.

| 상태 | 출구 | 바닥 링 | 출구 광원 | 미니맵 |
|---|---|---|---|---|
| 열쇠 없음 | 사다리 + 자물쇠 (`doorGroup` — 1층/2층 구조 도입 후 문 슬래브 대신 사다리, 아래 참고) | 호박색 | 호박색 | 호박색 |
| 열쇠 있음 | 회전하는 초록 포탈 (`cachedPortal`) | 초록 | 초록 | 초록 |

전환은 `applyKeyState()` 한 곳에서만 합니다. **광원 개수는 바뀌지 않습니다** —
`cachedExitLight`는 하나 그대로 두고 `color`만 바꿉니다(최우선 규칙 1·2번).
문과 자물쇠, 열쇠는 전부 emissive 재질이라 새 광원을 만들지 않습니다.

문은 `exitPos`의 **뚫린 이웃 쪽을 향해 고정**합니다. 돌아가게 하면 우스꽝스럽습니다.

열쇠는 시작 지점 기준 BFS 거리가 **최대치의 45% 이상**인 칸 중 무작위(출구 칸 제외).
획득 반경은 `KEY_PICK_R = 0.75`.

> ⚠️ **`keyPos`는 `buildWorld()`보다 먼저 정해야 합니다.**
> `buildWorld()`가 `keyPos`를 읽어 열쇠 메시를 놓습니다. 처음에 키 배치 코드를
> `buildWorld()` 뒤에 뒀더니 메시가 **직전 레벨의 열쇠 칸**(첫 판은 초기값 `(1,1)`)에
> 놓였습니다. 획득 판정은 `keyPos`를 직접 보므로 **상태 전이 테스트는 전부 통과했고**,
> 화면에만 열쇠가 없었습니다. 로직 검증만으로는 못 잡습니다 —
> **메시의 월드 좌표를 실제로 찍어 보십시오.**

---

## 1층/2층 구조

한 스테이지(`level`)가 미로 두 개로 늘어났다. 1층에서 열쇠를 찾아 사다리를 오르면
2층 미로가 새로 생성되고, 2층 출구에 도달해야 진짜로 스테이지가 끝난다(`onEscape()`).
난이도를 올리기 위한 변경이다.

**핵심 설계 — 새 위치 변수를 만들지 않는다.** `exitPos`는 층과 무관하게 "그 층의
목표 칸" 역할을 그대로 한다. 갈리는 지점은 딱 하나, 목표 칸에 열쇠를 들고 도달했을
때뿐이다(게임 루프의 "exit check"):

```js
if(hasKey){
  if(floor===1){climbLadder();return;}
  onEscape();return;
}
```

`floor`(1 또는 2)는 `level`(스테이지 번호)과 별개의 모듈 상태다. `climbLadder()`는
`floor=2`로 바꾸고 `startLevel(level)`을 그대로 다시 부른다 — **`startLevel()`을
층별로 쪼개지 않았다.** 기존 함수가 `floor`를 읽어서 몇 줄만 분기하는 방식으로
최소한만 건드렸다:

- 스폰 마릿수: `FLOOR_MON_COUNT(stage,floor)`/`FLOOR_CRAWLER_COUNT(stage,floor)`
  (`constants.js`) — 예전 `MON_COUNT(lv)=min(lv,4)`와 "레벨 3부터 크롤러" 규칙을
  완전히 대체했다. **1층 = 스테이지 번호만큼(전부 standard) / 2층 = 스테이지+1
  (크롤러 고정 1마리 + 나머지 standard).** 4마리 상한이 사라졌다 — 의도된 변경이다.
- 열쇠: 배치 코드는 그대로 두고(어차피 좌표만 계산, `keyMesh`는 안 보이면 그만),
  2층이면 그 뒤에 `hasKey=true` 한 줄만 추가한다. `applyKeyState()`가 손 안 대고도
  "항상 열린 포탈"을 보여준다 — 열쇠 도입 이전의 순수 포탈 흐름과 동일하다.
- `steps`는 1층에서만 리셋한다(2층은 이어서 센다). HUD의 STEPS도 하드코딩 0 대신
  `steps` 변수를 그대로 찍도록 바꿨다.
- HUD의 LEVEL 칸에 `1 · 1F` / `1 · 2F`처럼 층을 같이 표시한다.

**`floor`는 `startLevel()`이 스스로 관리하지 않는다.** 새 스테이지 시작·재시도·다음
스테이지 버튼(`startBtn`/`restartBtn`/`nextBtn`/`retryBtn`) 핸들러가 전부 `startLevel()`
호출 직전에 `floor=1`을 명시적으로 세팅한다 — 2층에서 죽었어도 재도전은 항상 그
스테이지의 1층부터 시작한다(사용자와 합의한 저장 규칙). `floor=2`를 세팅하는 곳은
`climbLadder()` 하나뿐이다.

**저장(`js/save.js`)은 손대지 않았다.** `resumeLevel`은 여전히 스테이지 번호만
가리킨다 — 새로고침하면 항상 그 스테이지의 1층부터. 층·열쇠 소지 상태는 저장하지 않는다.

### 사다리 — 문 재활용

`doorGroup` 변수명은 참조가 많아 **그대로 뒀다.** 안에 들어가는 메시만 문 슬래브
(`gDoorSlab`, 제거함)에서 사다리 레일+가로대(`gLadderRail`/`gLadderRung`)로 바꿨다.
자물쇠(`gLockBody`/`gLockShkl`, `mLock`)는 기존 것을 그대로 재사용 — 가로대 사이에
걸린 모습으로 자연스럽게 붙는다. 새 재질도 새 광원도 없다(둘 다 기존 `mDoor`/`mLock`
재사용).

**사다리 자체와 자물쇠는 서로 다른 것을 신호한다.** 사다리(`doorGroup`)는 "여기서
2층으로 간다"는 사실 자체를 나타내고, 자물쇠(`lockGroup`, `doorGroup`의 자식)는
"아직 못 올라간다"만 나타낸다. 그래서 `applyKeyState()`에서 둘을 분리했다 —
`doorGroup.visible=floor===1`(사다리는 1층에서 항상 보임), `lockGroup.visible=!hasKey`
(자물쇠만 열쇠 여부로 토글). 처음엔 문 시절처럼 `doorGroup` 전체를 `!hasKey`로
껐다 켰는데, 그러면 열쇠를 얻는 순간 사다리 자체가 사라져 버렸다 — 사용자 피드백으로
자물쇠만 없어지도록 고쳤다.

2층에는 이 사다리가 필요 없다(`doorGroup.visible=floor===1`이 자동으로 숨긴다).
`buildWorld()`는 층 구분 없이 사다리를 매번 만들지만, 숨겨지므로 낭비가 있어도
무해하다 — `keyMesh`를 층 구분 없이 항상 배치하는 것과 같은 이유·같은 패턴이다.
포탈(`cachedPortal`)도 마찬가지로 `floor===2`일 때만 보인다 — 1층에서는 열쇠를
얻어도 포탈로 바뀌지 않고 사다리가 계속 보인다(열쇠 이전 시절엔 문↔포탈 전환이
`hasKey` 하나로 결정됐지만, 지금은 `floor`가 "무엇을 보여줄지", `hasKey`는
"자물쇠를 보여줄지"로 역할이 나뉘었다).

> ⚠️ **이건 진짜 위아래로 뚫린 3D 건물이 아니다.** 사다리를 오르면 화면이 그대로
> 유지되는 게 아니라 레벨이 바뀔 때처럼 **미로를 통째로 교체**한다(`climbLadder()`가
> `startLevel()`을 다시 부름). 천장에 실제 구멍을 뚫어 2층이 훤히 보이게 하려면
> 병합된 천장 지오메트리를 칸 단위로 다시 손봐야 해서(5번 항목, 청크 병합 참고)
> 리스크가 훨씬 크다. 사다리는 "여기서 위로 간다"는 시각 장치일 뿐이고, 실제
> 전환은 순간적이다 — 그래서 짧은 화면 플래시(`#flash`, 기존 탈출/사망 때 쓰는 것과
> 동일한 요소·같은 방식)만 준다.

### 2층 분위기 — 텍스처만 다른 재질 세트

2층은 1층보다 더 우중충한 황토~주황색이고 핏자국도 더 많다(사용자 피드백으로 추가).
`makeWallpaper`/`makeCarpet`/`makeDropCeiling`(`js/textures.js`)에 `opts` 매개변수를
추가해 베이스 색·핏자국 개수·그라임 진하기를 바꿀 수 있게 했다 — **기본값은 1층과
완전히 동일**해서 옵션을 안 주면 이전 결과와 한 픽셀도 다르지 않다.

`wallMap2`/`floorMap2`/`ceilMap2`와 그걸 쓰는 `mWall2`/`mFloor2`/`mCeil2`/`mCeilDark2`를
1층 것들 옆에 나란히 만들어 두고, `buildWorld()`가 `floor` 값으로 재질 세트만 고른다
(`wallMat=floor===2?mWall2:mWall` 식). 청크 병합·광원·조명 파라미터는 전혀 안 건드려서
성능 특성은 1층과 동일하다 — 최우선 규칙 1번(광원 개수 고정)에 걸리지 않는다.

> ⚠️ **`mCeil.emissiveIntensity`를 만지는 곳(정전 켜짐/꺼짐, 레벨 시작)마다
> `mCeil2`도 같이 건드려야 한다.** 하나만 빠뜨리면 정전 중에 2층 천장만 계속 밝게
> 빛나는 버그가 난다 — 처음 구현할 때 실제로 이 부분을 놓칠 뻔했다. `mCeil`을
> 검색해서 나오는 자리를 전부 확인하고 짝을 맞췄다.

---

## 벽 핏자국과 스프레이 화살표

### 핏자국 — `makeWallpaper()`에 추가한 얼룩 한 겹

기존 "물 얼룩" 패스 바로 뒤에 같은 `blob()` 헬퍼로 마른 핏빛(`rgba(70,18,14,...)`) 얼룩을
추가하고, 얼룩 아래로 흘러내린 드립 자국을 `createLinearGradient`로 몇 가닥 더합니다.

**개수를 물 얼룩(9개)보다도 적게(1~2개) 둔 이유** — 이 텍스처는 모든 벽 세그먼트에
**동일하게** 입혀집니다(칸마다 다시 그리지 않고 재질 하나를 공유). 개수를 올리면
복도마다 똑같은 자리에 피가 있는 게 반복 무늬로 눈에 띄어 "벽지 패턴"처럼 싸구려로
보입니다(물 얼룩 항목의 기존 주석과 같은 이유). 색도 선명한 빨강이 아니라 어두운
갈색에 가깝게 낮춰 "마른 지 오래된 자국"으로 읽히게 했습니다.

### 스프레이 화살표 — `makeArrowDecal()` + 레벨당 무작위 배치

바닥에 눕혀 붙이는 알파 데칼입니다. 실제 길찾기 힌트가 아니라 **분위기용 장치**로
설계했습니다 — 사용자와 상의해 정한 방향입니다.

> ⚠️ **이 프로젝트는 "출구의 저주파 험을 의도적으로 제거해 미니맵을 유일한 단서로
> 남겼다"는 설계 원칙이 있습니다.** 화살표가 실제로 정확한 길을 알려주면 이 원칙이
> 깨집니다. 그래서 화살표는 **BFS가 아니라 직선(나침반) 방향**만 가리키고, 벽 구조를
> 전혀 모릅니다 — "이 미로를 먼저 헤맨 누군가가 벽 뒤에 뭐가 있는지도 모른 채 대충
> 그어 둔 낙서"라는 설정입니다. 갈림길·막다른 길 앞에서는 실제 출구 방향과 얼마든지
> 어긋날 수 있고, 이건 버그가 아니라 의도입니다. 미니맵은 여전히 유일한 정확한 단서입니다.

**텍스처(`makeArrowDecal`)** — 배경을 채우지 않는 투명 캔버스입니다(다른 세 텍스처
함수와 다른 점 — 바닥 재질 위에 겹쳐 그려지는 데칼이므로 알파가 있는 부분만 남아야
합니다). 화살대·화살촉을 매끈한 도형이 아니라 `dab()`(반투명 원 하나, `blob()`과
비슷하지만 텍스처 이음매 처리가 없는 단순판)를 수백 개 흩뿌려서 그립니다 —
스프레이 캔 특유의 거친 가장자리를 매끈한 `fillRect`/`fill()`로는 낼 수 없습니다.
캔 노즐 주변에 흩날리는 옅은 안개(오버스프레이)도 별도 패스로 더합니다.
캔버스 위쪽(화살촉 방향)이 항상 "가리키는 방향"이 되도록 그려 두고, 실제 회전은
전부 배치 코드가 담당합니다.

**배치(`index.html`)** — `startLevel()`에서 `pathCells`(시작·출구 칸 제외) 중
무작위 한 칸을 `arrowPos`로 고릅니다. `buildWorld()`가 이 값을 읽어 데칼을 놓으므로
**`keyPos`와 동일한 이유로 반드시 그 앞에서 정해야 합니다.**

방향 계산은 플레이어 yaw와 똑같은 공식을 재사용합니다:

```js
// gArrowDecal(평면)의 기본 법선은 +Z. rotation.x=-PI/2로 눕히면 로컬 +Z가 +Y(위)를
// 향해 바닥에 눕고, 이때 캔버스 위쪽(화살표가 그려진 방향)은 세계 -Z를 향한다.
// 바깥 그룹을 rotation.y=atan2(-dx,-dz)만큼 돌리면 — P.yaw와 정확히 같은 공식 —
// (dx,dz) 방향, 즉 출구 쪽을 가리키게 된다.
const adx=exitPos.x-arrowPos.x, adz=exitPos.z-arrowPos.z;
arrowGroup.rotation.y=Math.atan2(-adx,-adz);
```

`(dx,dz)`는 그리드 좌표 차이를 그대로 씁니다. 두 좌표계(그리드·월드)가 `CELL`
배율로만 다르므로 방향(각도) 계산에는 영향이 없습니다.

재질(`mArrow`)은 `mkMat({map,transparent:true,opacity:.9,side:DoubleSide})`로
만듭니다. 광원을 새로 만들지 않는 순수 장식이라 emissive는 주지 않았습니다 —
바닥 조명을 그대로 받아도 충분히 보입니다(실측 스크린샷으로 확인).

> **검증 시 주의할 점** — 화살표는 `arrowPos`에 눕혀진 평면이라 정면에서 보면 거의
> 안 보이고 위에서 내려다봐야 보입니다. 실기기 검증 없이 3D 렌더링 결과만 볼 때는
> 카메라 pitch를 충분히 낮춰(아래를 보게) 확인하십시오. 배치 칸이 어두운 구역
> (`isDarkZone`)이면 조명이 거의 없어 화살표가 안 보일 수 있는데, 이것도 버그가
> 아니라 어두운 구역 설계(검은 몬스터가 안 보이는 것과 같은 원리)의 자연스러운 결과입니다.

---

## 다음 작업 후보 (우선순위)

| 순위 | 작업 | 비고 |
|---|---|---|
| 높음 | **열쇠·문 백룸 리스킨** | 황금 열쇠 + 나무 문 + 자물쇠는 던전 물건이라 백룸에서 튄다. 사용자와 합의된 방향: 열쇠 → 사무실 출입증 카드, 문 → 회색 방화문 + 초록 EXIT 발광 표지판, 자물쇠 → 카드리더기(빨강 LED → 초록). `applyKeyState()` 구조와 "광원 개수는 안 바꾸고 색만" 정책은 그대로 두고 겉모습만 교체 |
| 중간 | UI 팔레트 정리 | body 배경 `#080a10`이 아직 던전 색. HUD 앰버가 노란 배경에서 대비가 약해지는지 확인 필요 |
| 중간 | 게임플레이 확장 | 스태미나·달리기, 은신 |
| 낮음 | ~~1층/2층 구조~~ | 완료 (열쇠→탈출이었던 흐름을 열쇠→사다리(1층)→탈출(2층)으로 확장. 몬스터 수식 `FLOOR_MON_COUNT`/`FLOOR_CRAWLER_COUNT`가 예전 `MON_COUNT` 완전 대체 — 1층=stage마리, 2층=stage+1마리(크롤러 고정 1). 4마리 상한 사라짐) |
| 낮음 | ~~몬스터 종류 분화 (크롤러 좀비)~~ | 완료 (레벨 3부터 스폰 슬롯 1개를 절지동물형 크롤러로 교체 — 시야 좁고 청각 넓음(걸어도 감지), 속도 +28%. 재질·광원 신규 없음) |
| 낮음 | ~~벽 핏자국 + 스프레이 화살표~~ | 완료 (핏자국은 `makeWallpaper()`에 얼룩 한 겹 추가. 화살표는 레벨당 무작위 길 칸 1곳, 직선 방향만 가리키는 분위기용 장치 — 미니맵이 유일한 단서라는 원칙 유지) |
| 낮음 | ~~정전 + 손전등~~ | 완료 (레벨 전체 천장등이 무작위로 꺼졌다 복구. 손전등은 광원 개수가 늘어나는 유일한 예외 — 실기기 프레임 확인 필요, 아래 항목 참고) |
| 낮음 | ~~상하 시야~~ | 완료 (`P.pitch`, `camera.rotation.order='YXZ'` 덕에 값 하나 추가로 끝남) |
| 낮음 | ~~몬스터 좌우 비대칭~~ | 완료 (왼팔만 길게 — 사용자와 합의된 방향. 움직임 쪽 변형은 보류) |
| 낮음 | ~~백룸 리스킨~~ | 완료 (배경·조명 반전 / 검은 실루엣 몬스터 / 거리 기반 걸음걸이 / 입과 이빨) |
| 낮음 | ~~열쇠·잠긴 문~~ | 완료 (기능. 겉모습은 위 항목) |
| 낮음 | ~~재질 경량화~~ | 품질 프리셋에 포함되어 완료 (LOW/MEDIUM = Phong) |

### 실기기에서 확인이 필요한 것

- **ULTRA가 이제 쓸 만한지.** 백룸 전환으로 광원이 11 → 8개로 줄었습니다. 예전에는 광원 11개
  때문에 `renderScale`이 하한 0.5에 붙어 HIGH(실효 배율 2.0)보다 흐렸는데, 8개면 달라질 수
  있습니다. 계측 패널의 `품질` 행이 노란색(하한)인지 보십시오.
- **형광등 버즈(`panelBuzz`)의 크기·빈도.** 횃불 탁탁 소리를 대체하며 새로 만든 소리라
  체감 조정이 안 되어 있습니다.
- **발소리 박자.** 거리 기반 위상으로 바뀌면서 예전보다 뜸하고 무거워졌습니다.
- **상하 시야 감도·한계.** `P.pitch` 감도를 좌우와 동일한 `0.011`로, 한계를 `PITCH_LIMIT≈60°`로
  두고 시작했습니다. 둘 다 임시값입니다 — 실기기에서 체감하며 조정이 필요합니다.
- **손전등이 켜진 채로도 프레임이 유지되는지.** `SpotLight`가 하나 늘어 HIGH 기준
  광원이 6 → 7개가 됩니다(정전 여부와 무관하게 씬에 상주). 정전 중엔 천장등 풀
  전체가 꺼지므로 실제 활성 광원 수는 오히려 평소보다 적지만, 그래도 실측이 먼저입니다.
- **정전 발생 빈도·지속시간·손전등 밝기.** `BLACKOUT_*`/`FLASH_*` 상수 전부 초기
  제안값입니다. 너무 자주/드물게 오지는 않는지, 12초가 길게/짧게 느껴지는지,
  손전등 원뿔이 너무 좁아/넓어 보이는지 체감이 필요합니다.
- **크롤러 좀비의 체감 난이도·다리 각도.** `MONSTER_TYPES.crawler`의 `sightR`(5)/
  `hearR`(6.5)/`hearMinSpd`(0)/`spdMul`(1.28)과 `CRAWL_UPPER_SPLAY`/`CRAWL_LOWER_WORLD`
  (다리가 꺾이는 각도) 전부 초기 제안값입니다. "걸어도 들린다"가 너무 가혹한지,
  좁은 시야 대신 넓은 청각이라는 트레이드오프가 체감되는지, 다리 실루엣이 의도한
  대로 거미처럼 보이는지 실기기 확인이 필요합니다. 등장 빈도(2층마다 고정 1마리,
  "1층/2층 구조" 항목 참고)도 마찬가지입니다.
- **1층/2층 구조의 체감 난이도·플레이 시간.** 한 스테이지가 미로 두 개로 늘어나
  플레이 시간이 대략 두 배가 됩니다. `FLOOR_MON_COUNT`가 4마리 상한 없이 스테이지
  번호를 그대로 따라가므로(예: 스테이지 6이면 2층에 7마리), 고스테이지에서 몬스터
  밀도가 너무 높아지지 않는지 실기기에서 확인이 필요합니다. 전환 플래시(`#flash`
  재사용, 180ms)가 너무 짧거나 밋밋하게 느껴지는지, HUD의 `1F`/`2F` 표시가 플레이
  중 눈에 잘 들어오는지도 마찬가지입니다.
- **2층 팔레트·핏자국 농도.** `wallMap2`/`floorMap2`/`ceilMap2`에 넘기는 색·`bloodMul`/
  `blood`/`grimeMul`/`stainChance` 전부 초기 제안값입니다. 헤드리스 스크린샷으로는
  1층과 뚜렷이 구분되는 것까지만 확인했고, 실기기에서 너무 어둡거나(가시성 저하) 핏자국이
  과해서 지저분해 보이지 않는지 체감이 필요합니다.
- **스프레이 화살표의 크기·불투명도·발견 빈도.** `gArrowDecal`(1.7m 정사각) /
  `mArrow.opacity=.9` 모두 초기값입니다. 헤드리스 스크린샷으로는 "밝은 곳에서 또렷하게
  보인다"까지만 확인했고, 실제 플레이 중 시야 각도(눈높이 1.22m, 위에서 내려다보는
  각도가 아님)에서 얼마나 눈에 띄는지, 레벨당 1개가 발견 빈도로 적절한지는 실기기
  체감이 필요합니다.

---

## 작업 시 유의사항

1. **큰 변경 전에 방향을 먼저 상의할 것.** 코드부터 작성하지 말고 접근 방식을 제안하고 확인받으십시오.
2. **버그는 수정 전에 원인을 먼저 설명할 것.** 무엇이 왜 잘못되었는지 보고한 뒤 고치십시오.
3. 조작감·감도 관련 수치는 사용자가 직접 체감하며 조정합니다. 임의로 바꾸지 마십시오.
4. 성능 관련 변경 후에는 **계측 오버레이 수치로 검증**하십시오.
5. `sw.js`의 캐시명을 변경하면 기존 사용자에게 업데이트가 전파됩니다. 배포 시 버전을 올리십시오.

---

## 로컬 실행

`file://`로 열면 Service Worker와 오디오가 동작하지 않습니다.

```bash
python3 -m http.server 8000
```
